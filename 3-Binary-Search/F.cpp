#include <iostream>
#include <cmath>

using namespace std;

int main() {
    long long a, b, k;

    cin >> a >> b >> k;

    long long l = a, r = b;
    while (l < r) {
        long long m = (l + r) / 2;

        long long x = 0;
    }
}